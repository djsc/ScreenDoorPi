{
  "name": "sdoorpi",
  "version": "1.1.0",
  "description": "This program runs on a raspberry pi. It connects to a Firebase Realtime Database and reads the last message posted by the user. It then posts it on a connected I2c display. It also updates the heartbeat timestamp in firebase so that the user knows if the program crashes.",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "run": "node build",
    "buildandrun": "tsc && node build",
    "installandbuildandrun": "npm install && tsc && node build",
    "installandbuild": "npm install && tsc"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/djsc/ScreenDoorPi.git"
  },
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/djsc/ScreenDoorPi/issues"
  },
  "homepage": "https://github.com/djsc/ScreenDoorPi#README",
  "dependencies": {
    "dotenv": "^10.0.0",
    "firebase": "^8.8.1",
    "i2c-bus": "^5.2.2",
    "log4js": "^6.3.0",
    "moment": "^2.29.1",
    "readline": "^1.3.0",
    "sleep": "^6.3.0"
  },
  "devDependencies": {
    "@types/i2c-bus": "^5.1.0",
    "@types/node": "^16.4.10",
    "@types/sleep": "0.0.8"
  }
}
